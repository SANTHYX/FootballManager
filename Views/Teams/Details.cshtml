@model FootballManagerApp.Models.Team

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Team</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TeamType)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TeamType)
        </dd>
    </dl>
</div>
<div>
    <button type="button" class="btn btn-primary"><a asp-action="Edit" asp-route-id="@Model.Id">Edit</a></button>
    <button type="button" class="btn btn-warning"><a asp-action="Index">Back to List</a></button>
</div>
